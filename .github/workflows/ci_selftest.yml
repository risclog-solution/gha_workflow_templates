name: Template Self-Test

on:
  workflow_dispatch:
  pull_request:

jobs:
  pre-commit-resolution:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify fallback default without config
        env:
          DEFAULT_PYTHON_VERSION: "3.9"
        run: |
          tmpdir=$(mktemp -d)
          output=$(
            cd "$tmpdir" &&
            ruby "$GITHUB_WORKSPACE/.github/scripts/resolve_pre_commit_python.rb"
          )
          echo "$output" | grep -Fx "python-version=3.9"
          echo "$output" | grep -Fx "source=default fallback"
      - name: Verify default_language_version.python resolution
        env:
          DEFAULT_PYTHON_VERSION: "3.9"
        run: |
          output=$(ruby .github/scripts/resolve_pre_commit_python.rb .github/fixtures/pre_commit_default_language.yaml)
          echo "$output" | grep -Fx "python-version=3.12"
          echo "$output" | grep -Fx "source=default_language_version.python"
      - name: Verify hook language_version resolution
        env:
          DEFAULT_PYTHON_VERSION: "3.9"
        run: |
          output=$(ruby .github/scripts/resolve_pre_commit_python.rb .github/fixtures/pre_commit_hook_language.yaml)
          echo "$output" | grep -Fx "python-version=3.11"
          echo "$output" | grep -Fx "source=hook language_version"
      - name: Verify workflow input overrides config
        env:
          DEFAULT_PYTHON_VERSION: "3.9"
          INPUT_PYTHON_VERSION: "3.13"
        run: |
          output=$(ruby .github/scripts/resolve_pre_commit_python.rb .github/fixtures/pre_commit_default_language.yaml)
          echo "$output" | grep -Fx "python-version=3.13"
          echo "$output" | grep -Fx "source=workflow input"
      - name: Verify conflicting config fails fast
        env:
          DEFAULT_PYTHON_VERSION: "3.9"
        run: |
          if ruby .github/scripts/resolve_pre_commit_python.rb .github/fixtures/pre_commit_conflict.yaml; then
            echo "expected conflicting config to fail"
            exit 1
          fi

  generate-coverage-data:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install appenv stub
        run: |
          cp .github/fixtures/appenv_stub.sh ./appenv
          chmod +x ./appenv
      - name: Verify update-lockfile condition
        run: |
          py_le_310=$(python - <<'PY'
          import sys
          print("1" if sys.version_info[:2] <= (3, 10) else "0")
          PY
          )
          expected=$(python - <<'PY'
          import os
          v = os.environ["MATRIX_PY"]
          maj, min_ = (int(x) for x in v.split(".")[:2])
          print("1" if (maj, min_) <= (3, 10) else "0")
          PY
          )
          if [ "$py_le_310" != "$expected" ]; then
            echo "update-lockfile condition mismatch: python=${{ matrix.python-version }} computed=${py_le_310} expected=${expected}"
            exit 1
          fi
        env:
          MATRIX_PY: ${{ matrix.python-version }}
      - name: Verify update-lockfile execution
        env:
          APPENV_MARKER: .appenv_update_lockfile_called
        run: |
          rm -f "$APPENV_MARKER"
          py_le_310=$(python - <<'PY'
          import sys
          print("1" if sys.version_info[:2] <= (3, 10) else "0")
          PY
          )
          if [ "$py_le_310" = "1" ]; then
            ./appenv update-lockfile
          fi
          if [ "$py_le_310" = "1" ] && [ ! -f "$APPENV_MARKER" ]; then
            echo "expected update-lockfile to run but marker was not created"
            exit 1
          fi
          if [ "$py_le_310" = "0" ] && [ -f "$APPENV_MARKER" ]; then
            echo "unexpected update-lockfile marker for python=${{ matrix.python-version }}"
            exit 1
          fi
      - name: Generate minimal coverage data
        env:
          COVERAGE_FILE: .coverage.selftest.${{ matrix.python-version }}
        run: |
          python -m pip install --upgrade coverage[toml]
          python -m coverage run .github/fixtures/coverage_sample.py
      - name: Upload coverage RAW data
        uses: actions/upload-artifact@v4
        with:
          name: coverage-data-selftest-${{ matrix.python-version }}
          include-hidden-files: true
          path: ".coverage*"

  coverage:
    needs: generate-coverage-data
    uses: ./.github/workflows/coverage.yml
    with:
      min_coverage: 0
